#include "../include/candidate_synthesizer.h"

bool CandidateSynthesizer::synthesize(const std::filesystem::path& loopSummaryPath, const std::filesystem::path& grammarsDirectory, const std::filesystem::path& resultPath, const std::filesystem::path& candidatePath, SynthesisMode& synthesisMode) {
    return true;
}

bool CandidateSynthesizer::parse(const std::filesystem::path& candidatePath, const std::filesystem::path& grammarsDirectory, const std::filesystem::path& resultPath) {
    return true;
}
